---
title: "Receive real-time call events with webhooks"
description: "Configure a webhook URL on your agent to receive call_started, call_ended, call_analyzed, and tool_invoked events in real time."
---

Verbeo sends webhook events to your HTTPS endpoint as calls progress. You configure one URL per agent and Verbeo delivers all lifecycle events there automatically. This lets your systems react in real time — open a CRM record when a call starts, log duration when it ends, and push extracted insights to your database when analysis completes.

## Event types

| Event | Fires | Delivered to |
|---|---|---|
| `call_started` | Immediately when the call is answered | Agent `webhookUrl` |
| `call_ended` | Immediately when either party disconnects | Agent `webhookUrl` |
| `call_analyzed` | 20–30 seconds after `call_ended` | Agent `webhookUrl` |
| `tool_invoked` | During the call, when the agent invokes a custom tool | The tool's own endpoint URL |

All four events carry the same `call_id` for a given call. Use it as your primary key to correlate events across your systems.

## Set your webhook URL

In the Agent Editor, open **Settings** and fill in the **Webhook URL** field with your HTTPS endpoint. You can also set it via the API:

```bash
curl -X PATCH "https://api.verbeo.ai/agents?agentId=YOUR_AGENT_ID" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "config": {
      "settings": {
        "webhookUrl": "https://api.yourcompany.com/verbeo-events"
      }
    }
  }'
```

<Warning>
  Verbeo only delivers to HTTPS endpoints. Plain HTTP URLs are rejected. Your endpoint must present a valid TLS certificate and respond with a `2xx` status promptly — Verbeo's delivery request times out after about 12 seconds — or the event is retried.
</Warning>

## Event payloads

All lifecycle events (`call_started`, `call_ended`, `call_analyzed`) share the same top-level envelope:

```json
{
  "event": "call_started | call_ended | call_analyzed",
  "call_id": "b7a3c1e4-94f2-4c9d-a8e0-5d9f6c2a1b34",
  "agent_id": "agent_f3a2b1c4e5d6",
  "timestamp": "2026-04-17T11:03:15.000Z",
  "payload": { }
}
```

Dispatch in your handler using the top-level `event` field. For a field-by-field reference of every event, see the [Webhook events reference](/api-reference/webhooks/events).

### `call_started`

Fires within 1–2 seconds of the call being answered. Contains caller/callee numbers, call type, and any dynamic variables you passed when initiating the call.

```json
{
  "event": "call_started",
  "call_id": "b7a3c1e4-94f2-4c9d-a8e0-5d9f6c2a1b34",
  "agent_id": "agent_f3a2b1c4e5d6",
  "timestamp": "2026-04-17T11:03:15.000Z",
  "payload": {
    "status": "started",
    "start_time": "2026-04-17T11:03:15.000Z",
    "call_type": "outbound",
    "from_number": "+15550100001",
    "to_number": "+15550100002",
    "dynamic_variables": {
      "customer_name": "Sarah Chen",
      "plan": "premium"
    }
  }
}
```

### `call_ended`

Fires within 1–2 seconds of disconnect. Includes duration in seconds, cost in cents, and the disconnection reason. The transcript and extracted data are not yet available at this point.

```json
{
  "event": "call_ended",
  "call_id": "b7a3c1e4-94f2-4c9d-a8e0-5d9f6c2a1b34",
  "agent_id": "agent_f3a2b1c4e5d6",
  "timestamp": "2026-04-17T11:06:42.000Z",
  "payload": {
    "status": "completed",
    "call_type": "outbound",
    "from_number": "+15550100001",
    "to_number": "+15550100002",
    "start_time": "2026-04-17T11:03:15.000Z",
    "end_time": "2026-04-17T11:06:42.000Z",
    "duration": 207,
    "cost": 12,
    "disconnection_reason": "user_hangup",
    "dynamic_variables": {
      "customer_name": "Sarah Chen",
      "plan": "premium"
    }
  }
}
```

**Disconnection reasons:**

| Value | Meaning |
|---|---|
| `completed` | The call finished normally (default) |
| `user_hangup` | The human caller hung up |
| `agent_hangup` | The AI agent ended the call |
| `timeout` | Call exceeded the maximum duration |
| `voicemail` | Agent detected voicemail |
| `no_answer` | The called party did not pick up |
| `error` | A platform error ended the call |

`disconnection_reason` is an open string — Verbeo may return additional or more specific values over time, so treat any value you don't recognize gracefully rather than assuming a fixed set.

### `call_analyzed`

Fires 20–30 seconds after `call_ended`. This is the primary event for downstream integrations. It carries the full transcript, AI-generated summary, your extracted `analysis_data` fields, a log of every tool executed, and a recording URL.

```json
{
  "event": "call_analyzed",
  "call_id": "b7a3c1e4-94f2-4c9d-a8e0-5d9f6c2a1b34",
  "agent_id": "agent_f3a2b1c4e5d6",
  "timestamp": "2026-04-17T11:07:05.000Z",
  "payload": {
    "status": "analyzed",
    "data": {
      "id": "b7a3c1e4-94f2-4c9d-a8e0-5d9f6c2a1b34",
      "workspace_id": "5c1d9b27-3e4a-4f8b-bc12-7a9e0d4f6b1a",
      "agent_id": "agent_f3a2b1c4e5d6",
      "call_type": "outbound",
      "from_number": "+15550100001",
      "to_number": "+15550100002",
      "status": "completed",
      "duration": 207,
      "started_at": "2026-04-17T11:03:15.000Z",
      "ended_at": "2026-04-17T11:06:42.000Z",
      "disconnection_reason": "user_hangup",
      "recording_url": "https://recordings.example.com/call-b7a3c1e4.wav",
      "recording_sid": "RE9f8e7d6c5b4a3928170655e4d3c2b1a0",
      "call_sid": "CA1a2b3c4d5e6f70819a2b3c4d5e6f7081",
      "cost": 12,
      "created_at": "2026-04-17T11:03:14.000Z",
      "updated_at": "2026-04-17T11:07:05.000Z",
      "transcript": [
        { "role": "agent", "content": "Hi, how can I help you today?", "timestamp": "2026-04-17T11:03:18.000Z" },
        { "role": "user", "content": "Can you check the status of order ORD-98432?", "timestamp": "2026-04-17T11:03:22.000Z" },
        {
          "role": "tool",
          "tool_name": "lookup_order",
          "query": "{\"order_id\":\"ORD-98432\"}",
          "status_code": 200,
          "result": { "status": "shipped", "eta": "2026-04-19" },
          "error": null,
          "documents": [],
          "timestamp": "2026-04-17T11:03:26.000Z"
        },
        { "role": "agent", "content": "Your order shipped and arrives on April 19th.", "timestamp": "2026-04-17T11:03:30.000Z" }
      ],
      "transcript_clean": [
        { "role": "agent", "content": "Hi, how can I help you today?", "timestamp": "2026-04-17T11:03:18.000Z" },
        { "role": "user", "content": "Can you check the status of order ORD-98432?", "timestamp": "2026-04-17T11:03:22.000Z" },
        { "role": "agent", "content": "Your order shipped and arrives on April 19th.", "timestamp": "2026-04-17T11:03:30.000Z" }
      ],
      "summary": "Customer Sarah Chen called to check on order ORD-98432. The agent looked up the order, confirmed it had shipped, and gave an estimated delivery date of April 19th. The customer was satisfied and ended the call.",
      "analysis_data": {
        "issue_resolved": true,
        "sentiment": "positive"
      },
      "dynamic_variables": {
        "customer_name": "Sarah Chen",
        "plan": "premium"
      },
      "tools": [
        {
          "tool_name": "lookup_order",
          "tool_type": "custom",
          "description": "lookup_order executed (HTTP 200)",
          "args": { "order_id": "ORD-98432" },
          "result": { "status": "shipped", "eta": "2026-04-19" },
          "error": null,
          "executed_at": "2026-04-17T11:03:26.000Z"
        }
      ]
    }
  }
}
```

The `transcript` array interleaves `user`, `agent`, and `tool` turns in chronological order. A `tool` turn carries `tool_name`, `query`, `status_code`, `result`, `error`, and `documents`. The `transcript_clean` array is the same conversation with the `tool` turns removed — use it when you want only the human-readable dialogue. The `tools` array is a separate, flat log of every tool the agent executed, where `tool_type` is an open value such as `custom`, `native`, or `knowledge_base`.

<Note>
  The `recording_url` is a pre-signed, time-limited link. Download and store the audio in your own system if you need it beyond the signature window.
</Note>

### `tool_invoked`

Fires during the call, every time the agent invokes one of your custom tools. This event is sent directly to the tool's configured endpoint URL — not to the agent's `webhookUrl`.

```json
{
  "event": "tool_invoked",
  "call_id": "8eb0e5f2-e8b5-44cb-beba-9b802f39c340",
  "agent_id": "a2e0146d-6b37-4b09-b33b-f2c72f22684d",
  "timestamp": "2026-04-18T12:19:20.789Z",
  "payload": {
    "tool_name": "lookup_order",
    "args": {
      "order_id": "ORD-98432"
    },
    "call": {
      "call_type": "outbound",
      "customer_number": "+12125550001",
      "dynamic_variables": { "customer_name": "Sarah Chen" },
      "transcript": "[12:19:08] User: Can you check order ORD-98432?",
      "transcript_object": [
        { "role": "user", "content": "Can you check order ORD-98432?", "timestamp": "2026-04-18T12:19:08.000Z" }
      ]
    }
  }
}
```

Your handler must respond within 10 seconds with JSON. See the [Custom tools guide](/guides/custom-tools) for the full response format.

## Handle webhooks reliably

### Respond with 2xx immediately

Return a `200 OK` as fast as possible — Verbeo's delivery request times out after about 12 seconds. If your processing takes longer, queue the payload and acknowledge immediately:

```javascript
app.post('/verbeo-events', (req, res) => {
  res.sendStatus(200); // Acknowledge immediately
  processEventAsync(req.body); // Queue for background processing
});
```

### Make your handler idempotent

Verbeo retries events if your endpoint returns a non-2xx response or times out — a background reconciler re-drives any event that wasn't confirmed delivered, and you can resend manually from the dashboard (see [Monitor and retry deliveries](#monitor-and-retry-deliveries-in-the-dashboard)). The same event can therefore arrive more than once. Deduplicate on `(call_id, event)`:

```javascript
const processed = new Set();

async function handleEvent(body) {
  const key = `${body.call_id}:${body.event}`;
  if (processed.has(key)) return; // Already handled
  processed.add(key);

  switch (body.event) {
    case 'call_started':
      await createCallRecord(body);
      break;
    case 'call_ended':
      await updateCallDuration(body);
      break;
    case 'call_analyzed':
      await pushToCRM(body);
      break;
  }
}
```

In production, use a persistent store (Redis, database) rather than an in-memory set.

### Events may arrive out of order

Retries can cause `call_analyzed` to arrive before `call_ended` in rare cases. Each event is self-contained — process them independently rather than relying on order.

## Example Express.js webhook handler

```javascript
const express = require('express');
const app = express();
app.use(express.json());

app.post('/verbeo-events', async (req, res) => {
  // Acknowledge immediately
  res.sendStatus(200);

  const { event, call_id, payload } = req.body;

  switch (event) {
    case 'call_started':
      console.log(`Call ${call_id} started — caller: ${payload.from_number}`);
      await db.calls.create({ call_id, status: 'in-progress', ...payload });
      break;

    case 'call_ended':
      console.log(`Call ${call_id} ended — duration: ${payload.duration}s`);
      await db.calls.update(call_id, {
        status: 'ended',
        duration: payload.duration,
        disconnection_reason: payload.disconnection_reason,
      });
      break;

    case 'call_analyzed':
      const { data } = payload;
      console.log(`Call ${call_id} analyzed — summary: ${data.summary}`);
      await crm.updateContact({
        phone: data.to_number,
        call_summary: data.summary,
        ...data.analysis_data,
      });
      break;

    default:
      console.warn(`Unknown event type: ${event}`);
  }
});

app.listen(3000);
```

## Monitor and retry deliveries in the dashboard

Every delivery attempt is recorded, so you can confirm exactly what Verbeo sent and debug failures without adding any logging on your side.

Open **Call History**, click a call to open its details, and select the **Webhooks** tab. You'll see one row per delivery attempt, newest first, across all three lifecycle events.

| Column | What it shows |
|---|---|
| Event | `Started`, `Ended`, or `Analyzed` |
| Status | The HTTP status your endpoint returned — green for `2xx`, red otherwise. Shows `no response` on a timeout or network error, and `no URL` when the agent has no webhook configured |
| Source | How the attempt was triggered: `live` (real time), `cron` (automatic retry), `retry` (manual), or `failure` (failed-call path) |
| Time | When the attempt was made |

Expand a row to see the destination URL, a summary of the payload that was sent, and the response body or error your endpoint returned. Full URLs and payloads are visible to developer-role members; other roles see metadata only. Verbeo keeps the most recent 50 attempts per call.

### Retry a delivery

If your endpoint was down, click **Retry**, choose which events to resend (**All**, **Started**, **Ended**, or **Analyzed**), and click **Send**. Events are re-sent in lifecycle order and stop at the first failure, so a `call_analyzed` is never delivered ahead of a still-failing `call_ended`. The tab then polls for a few minutes to surface the new attempts as they land. Manual retry is available to developer-role members.

<Note>
  Verbeo also retries failed deliveries automatically in the background, so a transient outage usually resolves itself. Use manual retry when you've fixed your endpoint and want the data redelivered right away.
</Note>

## Security recommendations

- **Verify the `event` field** before processing. Unknown events should be ignored, not errored.
- **Use HTTPS with a valid certificate.** Plain HTTP endpoints are rejected before any events are sent.
- **Authenticate inbound requests** by adding a shared secret to the agent's webhook URL as a query parameter (e.g. `https://api.yourcompany.com/webhooks?secret=abc123`) and validating it in your handler.
- **Verbeo does not currently sign webhook payloads.** There is no HMAC signature header to verify — rely on the shared-secret query parameter above, and optionally allow-list Verbeo's egress IP range (contact [support@verbeo.ai](mailto:support@verbeo.ai) for the current addresses).
- **Do not log full transcripts** to services that cannot handle PII. Transcripts may contain names, phone numbers, emails, and other personal data.
- **Store `recording_url` contents promptly.** The pre-signed URLs expire — download recordings to your own storage if you need them for compliance or QA.
